package leetcode

import "sort"
func threeSumClosest(nums []int, target int) int {
	sort.Ints(nums)
	n:=len(nums)
	for first := 0; first < n; first++ {
		second:=first+1
		third:=n-1
		i {
			
		}
	}
}